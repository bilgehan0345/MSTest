; PlatformIO Project Configuration — baud_scanner
; CAN bus baud rate auto-scanner for ESP32.
;
; Build:  pio run
; Upload: pio run -t upload
; Monitor: pio device monitor

[env:baud_scanner]
platform      = espressif32
board         = esp32dev
framework     = arduino
monitor_speed = 115200
